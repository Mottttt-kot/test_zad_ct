#pragma once
#include <iostream>
#include <vector>
#include <algorithm>
#include <numeric>
#include <map>
void write_in_file(std::map<std::string, bool>& );
void algor(std::map<std::string, bool>&, std::map<std::string,long>&, long,long, long, long);
